import React from 'react'
import ReactDOM from 'react-dom/client'
import { LaMPlatform } from './product-app/LaMPlatform'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LaMPlatform />
  </React.StrictMode>,
)
